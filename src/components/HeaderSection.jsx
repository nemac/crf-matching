
import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";


export default styled(Box)(({ theme }) => ({
  height: '200px',
  alignContent: 'center',
  textAlign: 'center',
}));

